#to get an input from user.
father_name = input("What is your father's name? ")
print("My father's name is ", father_name)

age = int(input("What is your age?"))   #it considers the input as string by default so we must mention it like this to change it.
print("My age is", age)

